import './styles.scss';
import View from './App/App';

const root = document.querySelector('#root') as HTMLElement;

// eslint-disable-next-line no-unused-vars
const canvas = new View({}, root);
// canvas.drawSmthng();
